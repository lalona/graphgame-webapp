import { AceptOnlyNumbersDirective } from './acept-only-numbers.directive';

describe('AceptOnlyNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new AceptOnlyNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
